import * as DialogComponent from '@radix-ui/react-dialog'
const Modal = DialogComponent.Root
const ModalTrigger = DialogComponent.Root
const ModalContent = DialogComponent.Content

export { Modal, ModalContent, ModalTrigger }
